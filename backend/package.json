{
  "name": "sentient-audit-backend",
  "version": "0.1.0",
  "description": "AI Financial Audit System Backend",
  "main": "src/main.py",
  "scripts": {
    "dev": "uvicorn main:app --reload --host 0.0.0.0 --port 8000",
    "start": "uvicorn main:app --host 0.0.0.0 --port 8000",
    "test": "pytest"
  },
  "dependencies": {
    "fastapi": "^0.104.1",
    "uvicorn[standard]": "^0.24.0",
    "langgraph": "^0.0.20",
    "pydantic": "^2.5.0",
    "httpx": "^0.25.2",
    "tavily": "^0.1.0",
    "python-dotenv": "^1.0.0",
    "plotly": "^5.17.0"
  },
  "devDependencies": {
    "pytest": "^7.4.3",
    "black": "^23.10.1",
    "flake8": "^6.1.0"
  }
}